server:
  # 端口
  port: 10086
spring:
  application:
    # 当前应用的名称
    name: eureka-server
eureka:
  client:
    # 注册中心的地址
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    # 关闭注册自己
    fetch-registry: false
    register-with-eureka: false